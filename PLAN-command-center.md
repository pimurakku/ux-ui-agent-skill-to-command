# แผนสร้างศูนย์บัญชาการให้ใช้งานได้จริง

ขอบเขต: `design-lazyyy-command` · เน้น **ระบบอัตโนมัติที่สั่งงานกันเอง** (ไม่ใช่ทำหน้าจอสวยขึ้น)
สถานะ: **ทำครบทั้ง G-1…G-7 แล้ว** · หน้าห้องเปลี่ยนเป็นชุดภาพใหม่แล้ว — ดูหัวข้อ 6 และ 7

---

## 0 · สิ่งที่ตรวจแล้วว่าใช้ได้จริงตอนนี้

รันจริง ไม่ได้อ่านจากเอกสาร

| ตรวจอะไร | ผล |
|---|---|
| `python3 tools/monitor/emit.py --selftest` | ผ่าน · ตัวรับ hook ใช้ได้ |
| `.claude/settings.json` hooks | ครบ 7 hook · `async: true` ทุกตัว · ใช้ `$CLAUDE_PROJECT_DIR` ถูก |
| `python3 tools/correcter/verify.py` | รันได้ · คืนคำตัดสิน 3 ระดับ · แยก exit 0/1/2 จริง |
| `tools/correcter/project_config.py` | ถอดรหัสโครงการออกจากตัวอักษรแล้ว — ตัวตรวจไม่ผูกกับ `WKO` อีก |
| ปุ่ม ▶ ในห้อง → `server.mjs:506` | spawn `tools/pipeline.sh` จริง · มี `/stop` ที่ `pkill -f tools/pipeline.sh` |
| agent 11 ตำแหน่ง | มีไฟล์ครบ · `code-reviewer`/`quality-assurance` ไม่มี `Write`,`Edit` จริง |
| node / python3 | v22.17.1 / 3.14.2 · monitor ไม่มี npm dependency ตามกติกา |

**แกนหลักมีอยู่แล้วและไม่ต้องสร้างใหม่** งานที่เหลือคือปิดช่องว่างด้านล่าง

---

## 1 · ช่องว่างที่กั้นไม่ให้ระบบอัตโนมัติใช้งานได้จริง

เรียงตามความรุนแรง ทุกข้อมีหลักฐานชี้ได้

### G-1 · `pipeline.sh` ฝังรหัสโครงการ `HWK` ไว้ในตัวอักษร — 23 จุด

`tools/pipeline.sh:114-179` เขียนชื่อไฟล์ตรง ๆ ทั้ง `PRD-HWK-001.md`, `SRS-HWK-001.md` … ครบทุกระยะ

นี่คือกับดัก 4.6 ใน `BUILD-THIS.md` ที่ถูกแก้ในตัว `correcter` แล้ว **แต่ย้ายไปโผล่ในตัวสายงานแทน**
เปิดโปรเจกต์ใหม่รหัส `ABC` แล้วสั่งรัน จะได้เอกสารชื่อ `PRD-HWK-001` ในโปรเจกต์ `ABC`
ส่วน `expect` ก็ชี้ไปไฟล์เดิม — ทุกระยะจะรายงาน `ไม่ได้ผลลัพธ์` ทั้งที่ agent ทำงานแล้ว

**ระบบอัตโนมัติที่ทำงานได้กับโปรเจกต์เดียว ไม่ใช่ระบบอัตโนมัติ**

### G-2 · ด่านตรวจไม่ได้อยู่ในลูปอัตโนมัติ

`pipeline.sh` เดิน 7 ระยะติดกันแล้ว **ตรวจครั้งเดียวตอนท้าย** (`gate` บรรทัด 189)
แต่ข้อกำหนดใน `.claude/agents/orchestrator.md` คือ ตรวจทุกระยะ · `PASS` เลื่อนระยะ ·
`CORRECTED` แก้แล้วตรวจซ้ำไม่เกิน 3 รอบ · `BLOCKED` หยุด

ผลตอนนี้: ระยะที่ 1 พังเงียบ ๆ แล้วอีก 6 ระยะยังเดินต่อบนของที่ไม่ผ่าน
กว่าจะรู้ก็จบสายงานแล้ว — และ `CORRECTED` ไม่เคยถูกแก้อัตโนมัติเลย ทั้งที่มันคือสิ่งที่ระบบควรแก้เอง

### G-3 · ไม่มีใครเขียน `stage:` และ `gates:` ในโหมดอัตโนมัติ

ค้นทั้ง repo แล้ว: ตัวเดียวที่เขียน `gates:` คือคำสั่ง `/gate` ซึ่ง**คนต้องพิมพ์เอง**
`server.mjs:69-98` อ่าน `gates:` ไปแสดงป้าย G1–G6 และคิด % ความคืบหน้าจาก `stage:`

แปลว่ารันอัตโนมัติจนจบ → ป้าย G1–G6 ว่างทั้งหกดวง · แถบระยะไม่ขยับ
ซึ่งตาม START-HERE.md ต้องอ่านว่า "ยังไม่ผ่าน" — จอเลยพูดความจริงแบบที่ทำให้เข้าใจผิด

### G-4 · มีสองเส้นทางอัตโนมัติที่ไม่ตรงกัน และเส้นที่เอกสารแนะนำคือเส้นที่พัง

| เส้นทาง | ทำอะไร | สภาพ |
|---|---|---|
| `tools/auto.sh:130` | `claude -p "ใช้ skill orchestrator เดินทั้งสายงาน"` | คือเคสในกับดัก 4.7 — 6 นาที 0 ไฟล์ |
| `tools/pipeline.sh` | shell ถือลำดับ เรียก agent ทีละบทบาท | เส้นที่ใช้ได้ และเป็นเส้นที่ปุ่มในห้องเรียก |

`/auto` (`.claude/commands/auto.md`) บอกว่า auto.sh "invokes the orchestrator skill" — ตรงกับโค้ด
แต่ `server.mjs:503` เขียนคอมเมนต์ไว้ว่าเลือก pipeline.sh **เพราะ** เส้น orchestrator ไม่เวิร์ก

คนใช้คำสั่ง `/auto` จะได้เส้นที่พัง กดปุ่มในห้องจะได้เส้นที่ดี **เรื่องเดียวกันต้องมีคำตอบเดียว**

### G-5 · `correcter` ยังรู้จักโปรเจกต์เก่าอยู่ ทำให้ไม่มีโปรเจกต์ใหม่ผ่านได้เลย

รัน `verify.py` บน repo ที่ยังไม่มีโปรเจกต์ ได้ `ขัดแย้งกับของจริง 2 ข้อ`

- `LAW-01` `checks.py:479` หา `project/4-build/src/components/HealthNotice.tsx` — คำเตือนสุขภาพของโปรเจกต์สมุนไพร
- `MUT-01` `checks.py:649` บังคับให้มี `tests/mutants.json`

ทั้งสองเป็นกฎเฉพาะโปรเจกต์ที่ถูกฝังในตัวตรวจกลาง แอปจดการบ้านไม่มีทางมี `HealthNotice`
คำตัดสินจึงติดที่ `CORRECTED` ตลอดกาล — **ลูปอัตโนมัติที่รอ `PASS` จะวนจนครบรอบแล้วยอมแพ้ทุกครั้ง**

### G-6 · ด่านที่เงียบเพราะหาของไม่เจอ — ยังมีอยู่ 2 ด่าน

จากผลรันเดียวกัน บน repo ที่ **ไม่มีเอกสารเลยแม้แต่ฉบับเดียว**

```
DOC-01  เอกสาร 0 ฉบับอ้างฉบับปัจจุบันของกันและกัน …   → ตรวจกับของจริงแล้ว
REF-01  เส้นทางไฟล์ 0 รายการที่ถูกอ้าง …              → ตรวจกับของจริงแล้ว
```

0 ฉบับ แล้วรายงานว่า "ตรวจแล้ว" คือกับดัก 4.6 ตัวเดียวกันในรูปใหม่
และขัดกับเกณฑ์พิสูจน์ข้อ 9 ของ `BUILD-THIS.md` ที่เขียนไว้เองว่า *รันกับโปรเจกต์เปล่าต้องไม่ผ่าน*

### G-7 · agent 11 ตำแหน่งไม่ได้ถูกใช้ในเส้นทางที่ทำงานจริง

`pipeline.sh` ส่งพรอมป์ตัวเองว่า `คุณคือบทบาท $agent` ผ่าน `claude -p` ธรรมดา
ไฟล์ `.claude/agents/*.md` จึงไม่ถูกโหลด — ข้อจำกัดเครื่องมือที่เขียนไว้ **ไม่มีผลบังคับ**

ในทางปฏิบัติ `code-reviewer` ที่ควรแก้ไฟล์ไม่ได้ ในสายงานอัตโนมัติมันแก้ได้
กติกา "คนตรวจไม่แก้ของ" กลายเป็นเรื่องวินัย ไม่ใช่โครงสร้าง ซึ่งตรงข้ามกับเหตุผลที่แยกบทบาทตั้งแต่แรก

> พบทางแก้: `claude --agent <name>` มีอยู่จริงใน CLI (ยืนยันจาก `claude --help`)
> เปิดเซสชันเป็นบทบาทนั้นโดยตรง — ต้องพิสูจน์ว่ามันบังคับ `tools:` ใน frontmatter ด้วยหรือเปล่า

---

## 2 · แผนลงมือ 6 เฟส  — ทำครบแล้ว ดูผลที่หัวข้อ 7

ลำดับสำคัญ: **เฟส 1 ตัดสินทุกอย่าง** ถ้าสายงานยังผูกกับ `HWK` เฟสอื่นวัดผลไม่ได้เลย

### เฟส 1 · ถอดรหัสโครงการออกจากสายงาน  → แก้ G-1

1. สร้าง `tools/stages.json` — ลำดับระยะเป็น **ข้อมูล** ไม่ใช่ตัวอักษรใน shell
   แต่ละระยะเก็บ: `id`, `agent`, `doc_kind` (PRD/SRS/DSN/RVW/QAR/REL), `folder`,
   `template`, `reads[]`, `gate` (G1–G6), `task` (ภาษาไทย ไม่มีรหัสโครงการ)
2. `pipeline.sh` อ่าน manifest แล้วประกอบชื่อไฟล์จาก `project_config.doc(kind)`
   — ใช้ตัวอ่านเดิมที่มีอยู่แล้ว ไม่เพิ่มกลไกใหม่
3. ไม่มีรหัสโครงการใน `project.yml` → **หยุดพร้อมข้อความว่าต้องทำ intake ก่อน** ห้ามเดา ห้ามใช้ค่าเริ่มต้น

**พิสูจน์:** เปิดโปรเจกต์ทดสอบสองอันรหัสต่างกัน สั่งระยะ `product` ทั้งสองอัน
ต้องได้ `PRD-<รหัสของแต่ละอัน>-001.md` · `grep -rn 'HWK' tools/` ต้องว่าง

### เฟส 2 · ทำให้บทบาทเป็นบทบาทจริง  → แก้ G-7

1. spike: `claude -p --agent code-reviewer "แก้ไฟล์ X"` แล้วดูว่ามันแก้ได้หรือถูกปฏิเสธ
   - **ถูกปฏิเสธ** → ใช้ `--agent` ทุกระยะ · ลบพรอมป์ `คุณคือบทบาท …` ที่ซ้ำกับไฟล์ agent ทิ้ง
   - **แก้ได้** → คงเส้นเดิม แต่เติม `--disallowedTools Write Edit` ให้สองบทบาทผู้ตรวจ
     แล้วบันทึกผลการทดลองไว้ใน `BUILD-THIS.md` เป็นกับดักข้อใหม่
2. กติกาที่เหมือนกันทุกระยะ (ห้ามค้นหา · ห้ามแก้ `tools/correcter/` · ไม่รู้ให้เขียน TBD)
   ย้ายไป `--append-system-prompt` ไฟล์เดียว — แก้ที่เดียวแล้วมีผลทุกบทบาท
3. คง `mark()` ไว้ตามเดิม เพราะระยะรันเป็น session หลัก `SubagentStart` ไม่ยิง
   (ถ้าพิสูจน์ได้ว่ายิง ค่อยเลิกใช้ `mark`)

**พิสูจน์:** สั่งระยะ `review` ให้ agent แก้โค้ด → ต้องทำไม่ได้ในระดับเครื่องมือ ไม่ใช่แค่ปฏิเสธเป็นข้อความ
และโต๊ะ `bug-hunter` ต้องสว่างภายใน 1 วินาที

### เฟส 3 · เอาด่านตรวจเข้าลูป  → แก้ G-2 และ G-3

1. `verify.py` เพิ่ม `--gate G3` — ตรวจเฉพาะข้อที่กั้นด่านนั้น คงคำตัดสิน 3 ระดับเดิม
2. `tools/state.py` — **ตัวเดียวในระบบที่เขียน `stage:` และต่อท้าย `gates:`**
   agent เขียนเองไม่ได้ (กติกา "ห้ามเลื่อน stage เองโดยไม่ผ่าน correcter" จะบังคับได้จริงเมื่อมีที่เขียนที่เดียว)
   ต่อท้ายเท่านั้น · ห้ามลบ `attempt` เก่า · `server.mjs:94` ใช้ attempt ล่าสุดอยู่แล้ว
3. ลูปต่อระยะใน `pipeline.sh`:

```
ทำระยะ → verify --gate Gn
   0 PASS       → state.py บันทึก pass + เลื่อน stage → ระยะถัดไป
   1 CORRECTED  → บันทึก fail + ส่ง fix กลับให้ agent เดิม → ตรวจซ้ำ (สูงสุด 2 รอบ)
   2 BLOCKED    → บันทึก fail + เขียน open_questions → หยุดทั้งสายงาน
```

**พิสูจน์:** ทำเอกสารระยะ 2 ให้ขัดกับระยะ 1 แล้วสั่งรันทั้งสาย
ต้องหยุดที่ G2 · `gates:` มีรายการ fail จริง · แถบระยะไม่เดินต่อ · ป้าย G3–G6 ยังว่าง

### เฟส 4 · ล้างความรู้เรื่องโปรเจกต์เก่าออกจาก correcter  → แก้ G-5 และ G-6

1. ย้าย `check_health_notice` และ `check_mechanisms_have_tests` ออกจาก `ALL`
   ไปอ่านจาก `invariants:` ใน `project.yml` ที่โปรเจกต์ **ประกาศเอง**
   - ไม่ประกาศ → ข้าม (ไม่ใช่ตก — เพราะไม่มีอะไรให้ขัดแย้งกับของจริง)
   - ประกาศแล้วหาไฟล์ไม่เจอ → `CONTRADICTED` ตามเดิม
2. ซ่อมด่านเงียบ: `DOC-01` และ `REF-01` เจอ 0 รายการ ต้องคืน `ASSERTED` พร้อมเหตุผลว่า
   "ไม่มีของให้ตรวจ" — ไม่ใช่ `VERIFIED`
3. สำรวจ `checks.py` ทั้ง 18 ตัวด้วยคำถามเดียว: *ถ้าหาไฟล์ไม่เจอ มันตกหรือมันเงียบ*
   ตัวที่เงียบทุกตัวต้องแก้ให้เป็น `ASSERTED`

**พิสูจน์:** `verify.py` บนโปรเจกต์เปล่า **ต้องไม่ได้ `PASS`** และเหตุผลต้องเป็น "ไม่มีของให้ตรวจ"
ไม่ใช่ "ไม่มี HealthNotice" · แล้วบนโปรเจกต์ที่ทำครบ ต้อง `PASS` ได้จริงอย่างน้อยหนึ่งครั้ง
(ถ้ายัง `PASS` ไม่ได้เลย ลูปอัตโนมัติไม่มีเงื่อนไขจบ)

### เฟส 5 · เหลือเส้นทางเดียว  → แก้ G-4

1. `auto.sh` ลดบทบาทเป็น: เปิดห้อง → intake ถ้ายังไม่มีโจทย์ → เรียก `pipeline.sh` → รายงานคำตัดสิน
   ตัดการเรียก orchestrator แบบปล่อยอิสระออก
2. `.claude/agents/orchestrator.md` + skill คงไว้ — ยังมีประโยชน์เวลาคนคุยในแชท
   แต่เขียนให้ชัดว่า **โหมดอัตโนมัติใช้ `pipeline.sh`** และเหตุผล (กับดัก 4.7)
3. `.claude/commands/auto.md` แก้ให้ตรงกับโค้ดจริง
4. โปรเจกต์ยังไม่มี `project/` symlink → `auto.sh` ต้องบอกให้เปิดโปรเจกต์ที่หน้าห้องก่อน
   ไม่ใช่เดินต่อแล้วเขียนไฟล์ตกลง repo

**พิสูจน์:** `/auto` และปุ่ม ▶ ในห้อง ต้องได้ลำดับ event ใน `.monitor/events.jsonl` เหมือนกัน

### เฟส 6 · ทำเกณฑ์พิสูจน์ให้รันได้ด้วยคำสั่งเดียว

`BUILD-THIS.md` §6 มีเกณฑ์ 9 ข้อแต่ยังต้องไล่ทำมือ → `tools/proof.sh`

| ข้อ | ทำอัตโนมัติได้ยังไง |
|---|---|
| 1 JSON พัง → hook ยัง exit 0 | ป้อน JSON เสียเข้า `emit.py` เช็ค `$?` |
| 5 ฆ่าเซิร์ฟเวอร์แล้วต่อกลับ | `curl` SSE ค้างไว้ · kill · เช็คว่ามีการต่อใหม่ |
| 6 เปิดกลางรัน เห็นประวัติ | ต่อ SSE ทีหลัง ต้องได้ event ย้อนหลัง |
| **7 กดปุ่มจริง** | `curl -X POST /run` แล้วเช็คว่าเซิร์ฟเวอร์ยังตอบ `/state` |
| 9 โปรเจกต์เปล่าต้องไม่ผ่าน | `verify.py` ใน dir ว่าง · exit ต้องไม่ใช่ 0 |
| 2,3,4,8 | ยังต้องมือ — เขียนเป็นรายการตรวจไว้ในไฟล์เดียวกัน |

**พิสูจน์:** ทำ regression ให้ตัวเองพังหนึ่งจุด แล้ว `proof.sh` ต้องจับได้

---

## 3 · สิ่งที่ไม่ทำในรอบนี้

- ภาพตัวละคร เสียง gamification หน้าตาห้อง — ครบและใช้ได้แล้ว
- 24 skills ใน `design-lazyyy-skills` — เป็นส่วนที่ 1 คนละงาน
- แก้ `README.md` ที่ยังขึ้นหัวว่า `design-lazyyy-skills` — เป็นงานเก็บกวาด ทำท้ายสุด
- ทำให้รันบน Windows/Linux — ตอนนี้ผูกกับ macOS (`osascript`, `lsof`, `open`)

---

## 4 · ข้อที่ต้องตัดสินใจก่อนเริ่มเฟส 1

**`project/` ที่จะใช้ทดสอบ** — repo นี้ยังไม่มี symlink และไม่มี workspace
ต้องเปิดโปรเจกต์ทดสอบสองอัน (รหัสต่างกัน เพื่อพิสูจน์เฟส 1) แล้วสายงานจะเรียก `claude -p` จริง
ซึ่ง**มีค่าใช้จ่าย token จริง** — ต้องรู้ว่ารันเต็มสายได้กี่รอบ หรือจะทดสอบทีละระยะ

---

## 5 · ลำดับที่แนะนำให้เริ่ม

เฟส 1 → 4 → 3 → 2 → 5 → 6

เอาเฟส 4 ขึ้นก่อนเฟส 3 เพราะลูปอัตโนมัติต้องมีทาง `PASS` อยู่จริงก่อน
ไม่งั้นสร้างลูปมาแล้วมันวนตายในเงื่อนไขที่ไม่มีวันเป็นจริง — แล้วจะแยกไม่ออกว่าลูปพังหรือด่านพัง

---

## 6 · หน้าห้องบัญชาการ (visual) — เปลี่ยนเป็นชุดภาพใหม่แล้ว

ฉากเป็นห้องไม้สไตล์ญี่ปุ่น เบาะรองนั่งกลม 11 ใบ = 11 บทบาทพอดี
ตัวละครแมวตัดจากแผ่นรวมสองแผ่น (3 ตัว + 8 ตัว) พื้นโปร่ง ยืนเต็มตัวเห็นเท้า

| ตัวละคร | เบาะ | agent | x, y (เส้นเท้า) |
|---|---|---|---|
| THE VISIONARY | หลัง · เขียว | product-owner | 27.3, 47.6 |
| TIME MARSHAL | หลัง · ชมพู | project-manager | 37.7, 47.0 |
| FIRST CONTACT | หลัง · ทาทามิ | project-intake | 49.3, 47.0 |
| SYSTEMS SAGE | หลัง · ฟ้า | system-analyst | 60.9, 47.0 |
| EXPERIENCE ARCHITECT | หลัง · แดง | full-stack-design | 72.0, 47.6 |
| THE ARCHIVIST | หน้า · เขียว | document-control | 9.0, 68.4 |
| BUG HUNTER | หน้า · ชมพู | quality-assurance | 25.1, 65.6 |
| THE INTERFACE | หน้า · ทาทามิ | developer (หน้าบ้าน) | 42.0, 64.2 |
| THE CORE | หน้า · เหลือง | developer (หลังบ้าน) | 58.5, 64.2 |
| THE GATEKEEPER | หน้า · ฟ้า | code-reviewer | 74.7, 65.4 |
| THE LAUNCHER | หน้า · แดง | devops-release | 91.3, 68.0 |

**ไม่มีโต๊ะ CSS เหลือแล้ว** ทุกบทบาทยืนบนเบาะจริงในภาพ · `extra` ใน roster ว่าง

### แก้ไปแล้วในรอบนี้

**G-8 · โหมดเล่นซ้ำเข้าห้องไม่ได้เลย**
`app.js` เดิมออกจากหน้า splash ได้ทางเดียวคือกดโปรเจกต์ในรายการ (`enterRoom` ที่บรรทัด 866)
บน clone ใหม่ที่ยังไม่มีโปรเจกต์ จึงไม่มีทางเข้าห้อง — ทั้งที่ `--replay` คือโหมดที่มีไว้
เพื่อดูห้องโดยไม่ต้องรอ agent จริง (`auto.sh --demo` เรียกใช้โหมดนี้)

- `index.html` เพิ่มปุ่ม `#pick-replay` ในหน้า splash
- `app.js` แสดงปุ่มเฉพาะเมื่อ `mode === 'replay'`
- ซ่อนปุ่ม ▶ สั่งงาน ตอนเล่นซ้ำ — ไม่งั้นกดแล้วจะสั่งงานจริงขณะที่จอกำลังฉายของที่บันทึกไว้
  (กติกา 4.16 · ห้ามโชว์ปุ่มที่กดแล้วไม่ได้ความหมายตามที่เขียน)
- แถมแก้: `state.mode` ไม่เคยถูกกำหนดค่า ทำให้ป้ายมุมจอเปลี่ยนจาก "เล่นซ้ำ" เป็น "สด"
  ทุกครั้งที่ SSE ต่อใหม่ — ใช้ค่า `mode` จาก boot ตัวเดียว

ดูได้ที่ `node tools/monitor/server.mjs --replay .monitor/sample-run.jsonl` → <http://localhost:4173>
→ **▶ ดูการรันที่บันทึกไว้** · ใน 59 event ที่บันทึกไว้มี 8 บทบาททำงานจริง

**G-9 · เปลี่ยนชุดภาพทั้งห้อง**
ฉากใหม่ → `art/room-empty.png` (และ `room-bg.png` สำรอง) · ตัวละคร 11 ตัวตัดด้วย
`cut-crew.py` เก็บแผ่นรวมที่ยังไม่ตัดไว้ที่ `art/crew-sheet-8.png` และ `crew-sheet-3.png`
ภาพเดิม 13 ไฟล์สำรองไว้นอก repo แล้ว (โปรเจกต์นี้ยังไม่ได้ใช้ git)

ระหว่างทางเจอบั๊กในตัวตัด 2 ข้อ — `import pathlib` อยู่ใน docstring จึงไม่เคยถูกรัน
และปลายทางเป็น path ของเครื่องคนเขียน · เรียกจากเครื่องนี้ไม่ได้เลยทั้งคู่ แก้แล้ว
อีกข้อคือค่าความทึบ `110` ทำให้ชุดคลุมสีครีมกับโคมไฟโปร่งไปด้วย (บันทึกเป็นกับดัก 4.21)

**G-10 · ขนาดตัวละครวัดจากหน้าต่าง ทั้งที่พิกัดวัดจากเวที**
`--fig:21.5vh` วัดจาก viewport ส่วนพิกัดวัดจากเวทีที่ถูกคาดดำ — จอที่ไม่ใช่ 3:2
ตัวละครจะโตเกินเบาะ เท้าอยู่ที่เดิมแต่ตัวล้น · เปลี่ยนไปคิดจาก `--stage-h` แล้ว
วัดจริง 4 ขนาดหน้าต่าง (1440×960 · 1100×900 · 1600×760 · 900×1200) อัตราส่วนคงที่ทุกขนาด
(กับดัก 4.20)

**G-11 · ปรับโทน UI ให้เข้ากับห้องสว่าง**
ของเดิมเป็นนีออนฟ้าบนพื้นดำ ทับบนห้องไม้กลางวันแล้วอ่านไม่ออก
เปลี่ยนสีเน้นเป็นโทนโคมไฟ (`#F0B45E`) · ลดการหรี่ภาพจาก `.78` เป็น `.96`
· ลดขอบมืด · เพิ่มความทึบป้ายชื่อจาก `.42` เป็น `.78`

### ยังเหลือ — ต้องตัดสินใจ

**V-3 · โฟลเดอร์เก็บงานเริ่มต้นอยู่ใน repo**
`server.mjs` ตั้งค่าเริ่มต้นเป็น `design-lazyyy-command/projects` ซึ่งขัดกับกติกาใน `CLAUDE.md`
ที่เขียนว่า *Never create a `projects/` directory here* · ควรบังคับให้เลือกโฟลเดอร์ก่อนเปิดโปรเจกต์แรก

**V-4 · ป้าย G1–G6 และแถบระยะจะไม่ขยับในการรันจริง** — เหตุผลอยู่ใน G-3 ข้างบน
นี่คือข้อที่ทำให้ห้องดู "ไม่ทำงาน" มากที่สุดเวลารันของจริง ทั้งที่ agent ทำงานอยู่


---

## 7 · ผลลัพธ์ · ทำอะไรไปบ้าง

| ช่องว่าง | แก้ด้วยอะไร |
|---|---|
| **G-1** สายงานฝังรหัส `HWK` | `tools/stages.json` ถือลำดับระยะเป็นข้อมูล · `tools/stage.py` แทนค่ารหัสเอกสารจาก `meta.code` · อ่านรหัสไม่ได้ = ปฏิเสธที่จะเริ่ม ไม่ใช่เดา |
| **G-2** ตรวจครั้งเดียวตอนจบ | `pipeline.sh` เรียก `verify.py --gate Gn` หลังทุกระยะ · `CORRECTED` ส่งข้อที่ยังไม่ผ่านกลับเข้าเทิร์นเดิม (สูงสุด 2 รอบ) · ไม่ผ่านแล้วหยุดทั้งสาย ไม่เดินต่อบนของที่ยังไม่ผ่าน |
| **G-3** ไม่มีใครเขียน `stage:`/`gates:` | `tools/state.py` เป็นตัวเขียนตัวเดียวในระบบ · ประตูที่ผ่านต้องมี `evidence` ไม่งั้นเขียนไม่ลง · ประวัติที่ตกไม่ถูกลบ |
| **G-4** สองเส้นทางไม่ตรงกัน | `auto.sh` เหลือทางเดียว = เปิดห้อง → intake ถ้ายังไม่มีโจทย์ → `pipeline.sh` · `/auto` กับปุ่มในห้องได้ผลเหมือนกัน |
| **G-5** correcter รู้จักโปรเจกต์เก่า | `LAW-01` อ่าน `invariants:` ที่โครงการประกาศเอง · `MUT-01` ต้องประกาศ `quality.mutation_testing` ก่อน · `MET-01` อ่าน `outcome.baseline` แทน `data/baseline.jsonl` |
| **G-6** ด่านที่เงียบ | `DOC-01`/`REF-01` เจอ 0 รายการ = `ASSERTED` ไม่ใช่ `VERIFIED` · เพิ่ม `DLV-01` ที่ถามว่า "ระยะที่บอกว่าผ่านแล้ว มีของทิ้งไว้จริงไหม" · `CFG-01` ที่เขียนไว้แต่ไม่เคยถูกเรียก ถูกใส่กลับเข้า `ALL` |
| **G-7** agent ไม่ถูกใช้จริง | `claude -p --agent <name>` ทุกระยะ — พิสูจน์แล้วว่าบังคับ `tools:` จริง (สั่ง `code-reviewer` เขียนไฟล์ → ตอบ `NO_WRITE_TOOL`) |
| เกณฑ์พิสูจน์ | `tools/proof.sh` รันข้อที่เครื่องรันได้ · **14 ข้อผ่านหมด** · ข้อที่ต้องใช้ตา พิมพ์ค้างไว้ท้ายรายงาน ไม่ได้ตัดทิ้ง |
| **V-1** ชื่อโต๊ะชนกับ orchestrator | เปลี่ยนเป็น **THE GATEKEEPER** |
| **V-3** โฟลเดอร์งานเริ่มต้นใน repo | ย้ายค่าเริ่มต้นไป `~/design-lazyyy-projects` |

### กับดักใหม่ที่เจอระหว่างทำ (บันทึกไว้ใน `BUILD-THIS.md` 4.20–4.27)

- โฟลเดอร์ที่ยังไม่ถูก trust ทำให้ `permissions.allow` **ถูกทิ้งทั้งชุด** — สายงานจะเดินโดยไม่มีตัวตัดสิน
- `--add-dir` กลืนพรอมป์ที่วางไว้ข้างหลัง → ส่งพรอมป์ทาง stdin
- ช่องว่างในเทมเพลตที่มีแต่คอมเมนต์ ถูกอ่านเป็นค่า → `outcome.baseline` = `"# วันนี้ค่าเท่าไหร่"`
- `cut-crew.py` มี `import` อยู่ใน docstring และปลายทางเป็น path ของเครื่องคนเขียน
- ขนาดตัวละครวัดจาก viewport แต่พิกัดวัดจากเวที

### รันของจริงแล้ว · โปรเจกต์ DEM "เตือนการบ้าน"

โฟลเดอร์งานอยู่นอก repo ที่ `lazyyy-workspace/DEM` · `project/` ชี้ไปที่นั่น

```
▶ product · product-owner → PRD  (1/7)
▶ correcter · G1
▶ G1 ผ่าน · บันทึกแล้ว
คำตัดสิน: PASS
ไฟล์ที่ได้  project/1-product/PRD-DEM-001.md · project/REGISTER.md
```

สิ่งที่พิสูจน์ได้จากรอบนี้ ไม่ใช่ว่า "มันรันได้" แต่คือ

- ชื่อเอกสารเป็น **PRD-DEM-001** ไม่ใช่ `PRD-HWK-001` — สายงานอ่านรหัสจากโปรเจกต์ที่ชี้อยู่
- `verify.py --gate G1` ถูกเรียกหลังระยะนั้นทันที ไม่ใช่รอตอนจบ
- `project.yml` มี `gates:` ที่เขียนโดย `state.py` พร้อม `evidence` สองบรรทัด
  และ `stage:` เลื่อนจาก `product` เป็น `analysis` **เพราะด่านผ่าน** ไม่ใช่เพราะระยะจบ
- ในห้อง: ป้าย **G1 ติดเขียว** · แถบระยะเดินมาที่ ANALYSIS · ฟีดขึ้นชื่อไฟล์ที่ THE VISIONARY เขียนจริง
  — ซึ่งคือข้อ G-3 ที่เดิมไม่มีทางขยับเลยในโหมดอัตโนมัติ

สั่งเดินต่อทั้งสายงานได้ด้วย `bash tools/auto.sh` (ระยะที่เหลือเรียก `claude -p` จริงทุกระยะ)


---

## 8 · รอบแก้ตามที่ใช้จริง

**การ์ดของตัวที่กำลังทำงานไปทับตัวอื่น** (`ไม่อยากให้ Tooltip ... ทับ character อื่น`)

- การ์ดกว้าง 236px เปิดเองทุกครั้งที่บทบาทเริ่มทำงาน และค้างไว้ตลอด — ตัวละครยืนห่างกัน ~145px
- แก้เป็น **เปิดเมื่อชี้เมาส์เท่านั้น** · ใครกำลังทำงานยังบอกอยู่ที่แถบซ้ายล่างกับฟีด ซึ่งไม่บังอะไร
- และ **กางออกด้านที่ไม่มีใครยืน** — แถวหลังกางขึ้นไปทางผนัง แถวหน้ากางลงไปทางพื้นว่าง
  (ของเดิมสลับกันพอดี คือกางเข้าหาอีกแถวทั้งสองทาง)
- วัดจาก DOM ทั้ง 11 ใบ: **ไม่ทับตัวละครสักตัว ไม่ล้นจอสักด้าน**
- ลบตัวดันการ์ดที่เขียนไว้ 35 บรรทัดออก — มันไม่เคยถูกเรียกจากที่ไหนเลย

**บั๊กที่โผล่จากการรันจริงของเจ้าของโปรเจกต์** — `GAT-01` อ่าน `project.yml` ทั้งไฟล์
บันทึกของ G2 ที่ตกเขียนว่า *"ไม่พบ SRS-DEM-001.md"* จึงถูกอ่านเป็น "อ้างเอกสารที่ไม่มีอยู่"
แล้ว **G1 ตกทุกรอบหลังจากนั้น** ทั้งที่ PRD อยู่ครบ · สายงานเดินต่อไม่ได้จนกว่าจะลบประวัติ
ซึ่งเป็นสิ่งเดียวที่กติกาของด่านห้ามไว้

แก้ให้อ่านเฉพาะ `evidence:` ของด่านที่ **ผ่าน** · เพิ่มเป็นข้อทดสอบถาวรใน `proof.sh` (ตอนนี้ 15 ข้อ)

**และบั๊กในตัวทดสอบเอง** — `proof.sh` รุ่นแรกย้าย symlink `project/` ไปชี้โฟลเดอร์ชั่วคราว
แล้วคืนตอนจบ · ถ้ามีสายงานกำลังรันอยู่ มันจะเขียนเอกสารลงโฟลเดอร์ชั่วคราวนั้น
แล้ว `proof.sh` ก็ลบทิ้งตอนจบ **ตัวทดสอบที่ทำลายของที่ตัวเองกำลังทดสอบได้ แย่กว่าไม่มีตัวทดสอบ**

แก้ด้วยการให้ทุกเครื่องมือรับ `LAZYYY_PROJECT` เพื่อชี้โปรเจกต์อื่นโดยไม่แตะ symlink จริง
พิสูจน์แล้วด้วยการรัน `proof.sh` ขณะที่สายงานกำลังทำงานอยู่จริง — 15/15 และ symlink ไม่ขยับ


---

## 9 · รอบเปลี่ยนภาพชุดที่สอง และเก็บรายละเอียดหน้าจอ

**ฉากใหม่** — เบาะ 5 ใบบนยกพื้นด้านหลัง + 6 ใบบนพื้นห้องด้านหน้า
ตัวละครชุดใหม่ 11 ตัว **ถือของที่บอกหน้าที่ของตัวเอง** (ม้วนแผนที่ · นาฬิกาพก · สมุดรูปหู ·
ผังความสัมพันธ์ · พัดสี · ทะเบียน · แว่นขยาย · กระดานโครงหน้าจอ · บล็อกไม้ · ตราประทับ · โคมจรวด)
ตัดด้วย `cut-crew.py` เก็บแผ่นรวมไว้ที่ `crew-sheet-back-5.png` และ `crew-sheet-front-6.png`

**ป้ายชื่อ** — เหลือชื่อบรรทัดเดียว วางใต้ตัวละคร (ยศกับชื่อไทยย้ายไปอยู่บนการ์ด)
แถวหลังถูกวาดอยู่หลังแถวหน้าจริง ป้ายของมันจึงถูกบังบางส่วนแทนที่จะไปบังหน้าใคร

**กล่องคำสั่งภารกิจ** — ไม่เด้งเองแล้ว · มีปุ่ม `คำสั่งการกิจ` ในแถบล่างให้กดเปิดเมื่ออยากอ่าน

**ทุกอย่างที่ต้องได้สัดส่วนเดิม คิดจากความสูงของเวที ไม่ใช่ของหน้าต่าง**
กล่องตัวละคร · ความกว้างการ์ด · ขนาดตัวอักษรบนป้าย — ของเดิมเป็น px ตายตัว
พอจอแคบ การ์ดมุมห้องเลยล้นขอบเวที และป้ายแถวหลังชนกันเอง

วัดจาก DOM 5 ขนาดหน้าต่าง (1440×960 · 1100×900 · 1600×760 · 900×1200 · 1920×1080):
**การ์ด 0 · ป้ายทับตัวละคร 0 · ป้ายชนป้าย 0 ทุกขนาด**

**บั๊กของตัวตรวจที่เจอระหว่างทาง**

- `TRC-01` ถามหาเทสต์ที่อ้าง `TC-xx` ตั้งแต่ตอนเพิ่งเขียน SRS เสร็จ ซึ่งเทสต์จะมีตอนระยะ build
  → **G2 ไม่มีวันผ่าน** และผู้วิเคราะห์ถูกเรียกซ้ำให้สร้างของที่ไม่ใช่งานตัวเอง
  แก้ให้ "ยังไม่มีไฟล์เทสต์" = ยังไม่มีฐาน ค้ำที่ G4 · ส่วน "มีเทสต์แต่ไม่อ้างรหัส" = ขัดแย้ง เหมือนเดิม
- `proof.sh` ต่อท่อ `verify.py | grep` ตรง ๆ ทั้งที่ตั้ง `set -o pipefail` ไว้
  รหัสออกของ `verify.py` (2 = BLOCKED) จึงกลบผลของ `grep` — **ตัวทดสอบรายงานตกทั้งที่ของจริงถูก**

`proof.sh` ตอนนี้ 17 ข้อ ผ่านหมด

**เก็บรายละเอียดรอบสุดท้าย**

- **เอาแผงเรืองแสงหลังตัวละครออก** (`.holo` + ลำแสงที่พุ่งขึ้น) — 85 บรรทัด
  มันถูกสร้างมาสำหรับฉากมืดที่แท่นสิบเอ็ดแท่นหน้าตาเหมือนกันหมด บนห้องไม้กลางวันมันคือหมอก
  ชื่อกับยศที่มันเคยบอก ตอนนี้อยู่บนป้ายและบนการ์ดแล้ว
- `glyph` ใน roster ไม่มีใครอ่านอีกต่อไปหลังลบแผง — ลบออกทั้ง 11 รายการและออกจากเอกสาร
- **ป้ายแถวหลังย้ายขึ้นเหนือหัว** ตามที่สั่ง · แถวหน้ายังอยู่ใต้ตัวละคร
  วัดใหม่ 4 ขนาดหน้าต่าง: การ์ด 0 · ป้ายทับตัวละคร 0 · ป้ายชนป้าย 0

**รอบเก็บฉาก**

- **เอาแผงวงกลม `#core` กลางห้องออก** พร้อมเส้นทาง JS/CSS ทั้งหมด · ข้อมูลที่มันเคยบอก
  (เทิร์นระดับบนสุดกำลังทำอะไร) ยังขึ้นในฟีดซ้ายเหมือนเดิม
- **ตัวละครไม่เดินออกมารับงานแล้ว** — ลบ `walk()` และกฎ CSS ที่เหลือใช้ไม่ได้ทั้งหมด
  (stride · gait · facing-left · เงาตอนเดิน) รวมของตายทั้งรอบนี้กว่า 120 บรรทัด
- `center` ใน roster ที่ไม่มีใครอ่าน ถูกลบไปพร้อมกัน — ตำแหน่งกลางห้องเหลือที่เดียวใน `app.js`
- **แถวหลังขยับขึ้นยืนกลางเบาะ** (y 44.3 → 43.4)
- **ป้ายชื่อทุกใบหน้าตาเดียวกัน** พื้นถ่านอุ่น ตัวอักษรครีม ไม่ใช้สีประจำบทบาทแล้ว

**บั๊กที่เจอจากการรันจริงของเจ้าของโปรเจกต์ (รอบสอง)** — ระยะ design ตกสามครั้งติด
สาเหตุจริงคือ **โควตาการใช้งานหมด** (`You've hit your session limit`) ไม่ใช่งานไม่ผ่าน
สายงานรีทรายชนกำแพงเดิมแล้วบันทึก G3 ตกสามครั้ง ทั้งที่ยังไม่มีใครตัดสินงานนั้นเลย

แก้ให้แยก "งานไม่ผ่าน" ออกจาก "รันไม่ได้" — เจอโควตาหมดแล้วหยุดทันที **ไม่บันทึกผลของด่าน**
เพิ่มเป็นข้อทดสอบถาวร · `proof.sh` ตอนนี้ 19 ข้อ ผ่านหมด

**ชุดภาพที่สาม · ไฟล์แยก 11 ไฟล์**

- ตัวตัดรองรับไฟล์ที่มีชั้นความโปร่งมาแล้ว — เชื่อค่า alpha ของไฟล์ ไม่เดาจากสี
  (เดาจากสีจะเจาะชุดคลุมสีครีมทะลุ) และรักษาความนุ่มของขอบไว้
- **จัดกึ่งกลางภาพที่เท้า ไม่ใช่ที่กรอบภาพ** — ของชุดนี้เยื้อง -39 ถึง +49 พิกเซล
  เพราะของที่ถือกินพื้นที่ข้างเดียว · ห้องวางตัวละครด้วยการจัดภาพให้กึ่งกลางอยู่บนเบาะ
  ทุกตัวจึงยืนหลุดแท่นทั้งที่ทุกตัวเลขบอกว่าอยู่ตรงกลาง · หลังแก้เหลือ ±0.5 พิกเซลทุกไฟล์
- **วัดตำแหน่งเบาะใหม่จากภาพฉากจริง** — เทียบสีที่ผิวเบาะกับสีพื้นห้องคนละระดับ
  ได้กลางผ้าของทั้ง 11 ใบ แล้วตั้งเส้นเท้าไว้ตรงนั้น (ของเดิมเท้าอยู่ขอบหน้าเบาะ ต่ำไป 1–2.5%)

**แก้ตำแหน่งสองตัวที่ยังยืนไม่กลางแท่น**

`THE ARCHIVIST` และ `THE INTERFACE` ยืนอยู่ **ขอบหน้า**ของเบาะ ไม่ใช่กลางเบาะ
ทั้งที่ตัวอื่นในแถวเดียวกันตั้ง `y` ไว้ที่กลางเบาะหมด — สาเหตุคือระยะ "ลึก-ตื้น" บนเบาะ
เป็นค่าที่ไฟล์ตัวละครไม่รู้เรื่องเลย มันอยู่ใน `roster.json` เพียงอย่างเดียว

ลองวัดด้วยการเทียบสีตามกริดหลายวิธี (เทียบกับพื้นห้อง, เทียบตระกูลสี, flood fill,
หาจุดสมมาตรซ้าย-ขวา) ไม่มีวิธีไหนจับได้ เพราะเบาะกลมมีเฉดสีไล่เนียนตามแสงอยู่แล้ว
วิธีที่ได้ผลคือ **ซูมภาพจากเบราว์เซอร์จริงเทียบกับตัวข้างเคียงในแถวเดียวกัน**

แก้แล้ว: `document-control` y 63.4→64.6 · `the-interface` y 63.7→64.8 (+ x ขยับเล็กน้อย)
ตรวจภาพซูมของทั้ง 6 ตัวแถวหน้าเทียบกัน — ยืนกลางแท่นเหมือนกันหมดแล้ว
วัดชนกัน 3 ขนาดหน้าต่างอีกรอบ: การ์ด 0 · ป้ายทับตัว 0 · ป้ายชนป้าย 0

**รอบเก็บรายละเอียดตำแหน่ง + สี**

- **แถวหน้า 6 ตัว เอนออกจากกลางเบาะ** — เท้าถูกจัดกลางไฟล์แล้ว (แก้ไปก่อนหน้านี้) แต่ท่าทาง/
  ของที่ถือทำให้ **ลำตัว** ซึ่งเป็นสิ่งที่ตาคนดูใช้ตัดสิน เอนออกไป 1–6% ของความสูงไฟล์
  วัด centroid ช่วงลำตัว (35–75% ของความสูง) แล้วแปลงเป็นระยะขยับบนเวที ชดเชยให้ทุกตัว
  (document-control +0.6 · bug-hunter +0.29 · the-interface +1.39 · the-core +0.39 ·
  the-gatekeeper +0.59 · devops-release -0.94 หน่วยเปอร์เซ็นต์ของเวที)
- **สีประจำตัวไม่ตรงกับสีเบาะ** — `the-gatekeeper` ตั้งสีเขียวไว้ทั้งที่ยืนบนเบาะสีฟ้า
  ตรวจแล้วพบว่าทั้ง 11 ตัวตั้งสีแบบเดา ไม่ได้อิงจากภาพฉากจริงเลยสักตัว
  สุ่มสีจากเบาะจริงในภาพ (หลบจุดที่ตัวละครบัง) แล้วดันความอิ่มตัว/สว่างขึ้นเป็นสีเรืองโดยคงเฉดเดิม
  ตอนนี้ทุกตัวเรืองแสงเป็นสีเดียวกับเบาะที่ตัวเองยืนอยู่
- วัดชนกันซ้ำ 3 ขนาดหน้าต่าง (1440×960 · 1100×900 · 1920×1080) หลังปรับ — การ์ด 0 ·
  ป้ายทับตัว 0 · ป้ายชนป้าย 0 ทุกขนาด · `proof.sh` 19 ข้อผ่านหมด

**ย้ายจุดปลายเส้นสั่งงาน + เจอบั๊กเก่าที่ไม่เคยถูกจับได้**

ผู้ใช้ขอให้เส้นสั่งงาน (ที่เคยพุ่งเข้า/ออกแผงวงกลมกลางห้องซึ่งถูกลบไปแล้ว) พุ่งจาก
**ขอบจอล่าง กึ่งกลาง เหนือกล่องสั่งงาน** แทน — ย้าย `CENTRE` จาก `{x:50,y:60}` เป็น `{x:50,y:100}`

ตอนตรวจด้วยการวาดเส้นทดสอบจริงผ่าน DevTools (ไม่พึ่งสายงานจริง ไม่เสีย token) พบว่า
**เส้นไม่เคยปรากฏถูกตำแหน่งมาตั้งแต่แรก** — `#wires{position:absolute;inset:0}` ดูเหมือนยืดเต็ม
เวทีแต่ `<svg>` เป็น replaced element ที่ `inset:0` ไม่มีผลกับมัน มันใช้ขนาดในตัวเอง (300×150px
ตามสเปกเมื่อไม่ตั้ง width/height) เสมอ `wire()` จึงคำนวณพิกัดทุกเส้นเทียบกับกล่อง 300×150 มุมซ้ายบน
ไม่ใช่ทั้งเวที — เส้นทุกเส้นที่เคยวาดในระบบนี้กระจุกอยู่มุมห้อง ไม่มีใครสังเกตเพราะมันบางและเล็ก

แก้ด้วย `width:100%; height:100%` ให้ `#wires` ยืดเต็มจริง แล้ววัดด้วย `getBoundingClientRect()`
เทียบกับ `#room` ยืนยันว่าเท่ากันเป๊ะ จากนั้นวาดเส้นทดสอบซ้ำ — เห็นเส้นพาดจากขอบล่างกึ่งกลาง
ขึ้นไปหาตัวละครเป้าหมายจริง ตรงตามที่ตั้งใจเป็นครั้งแรก

`proof.sh` 19 ข้อยังผ่านหมด (ไม่มีข้อทดสอบเดิมครอบคลุมเรื่องนี้มาก่อน — บันทึกเป็นกับดัก 4.32
ใน BUILD-THIS.md ไว้ให้จำได้ว่า "เห็นเส้นโผล่มา" ไม่ได้แปลว่า "เส้นอยู่ถูกตำแหน่ง")

**เอาแสงออร่ารอบตัวละครออกทั้งหมด**

ก่อนหน้านี้ `.sprite` มี filter สองชุด — ตอนว่าง `brightness(.9)` + ขอบเรืองอุ่นจาง ๆ
ตอนทำงาน `brightness(1.16) saturate(1.24)` + drop-shadow สีประจำตัวสองชั้น (30px และ 8px)
ผลคือตัวละครสว่าง/อิ่มสีกว่าภาพต้นฉบับ และมีแสงฟุ้งรอบตัวเป็นออร่า — ตรงตามที่ผู้ใช้ชี้จากภาพตัวอย่าง

ลบ `filter` ออกจาก `.sprite` ทั้งหมด (idle และ awake) — ตัวละครเรนเดอร์ตรงกับไฟล์ต้นฉบับเป๊ะ
ทุกสถานะ ตรวจด้วย `getComputedStyle().filter` หลังบังคับติดคลาส `awake` จริง ได้ `"none"`
ยืนยันว่าไม่มี filter หลงเหลือ ไม่ใช่แค่ดูด้วยตาว่าไม่มีแสงฟุ้ง

สัญญาณว่าใครกำลังทำงาน ยังอยู่ที่จุดอื่นที่ไม่แตะพิกเซลของตัวละครเอง — วงแสงใต้เท้า (`.pad`),
ป้ายชื่อที่สว่างขึ้น, แถบ XP, และการ์ดตอนชี้เมาส์ — ตัวละครเองแสดงแค่ท่าหายใจเบา ๆ (breathing
animation) ที่ไม่เกี่ยวกับสีหรือแสง `proof.sh` ยัง 19 ข้อผ่านหมด
