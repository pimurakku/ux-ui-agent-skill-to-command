# เริ่มที่นี่ — ใบงานวันแรก

> เอกสารนี้เป็นภาษาไทยโดยตั้งใจ ไฟล์อื่นในโปรเจกต์เป็นภาษาอังกฤษตามกติกาใน `CLAUDE.md`
> ใบงานนี้เขียนให้นักเรียนอ่าน ไม่ได้เขียนให้เครื่องอ่าน

วันนี้คุณจะเปิดโปรเจกต์ของตัวเองหนึ่งอัน แล้วปล่อยให้ทีม agent ทำงานให้ดู
โดยคุณเป็นคนตั้งโจทย์ ไม่ใช่คนเขียนเอง

สิ่งที่ต้องได้กลับบ้าน **ไม่ใช่เอกสารสวย ๆ** แต่คือคำตอบของคำถามเดียว —
*ระบบนี้รู้ได้ยังไงว่างานที่ทำออกมาใช้ได้*

---

## 0 · เปิดห้อง

```sh
node tools/monitor/server.mjs --allow-run
```

เปิด <http://localhost:4173>

หน้าแรกคือ **ทางเข้า** มีสามอย่าง
- โฟลเดอร์ที่เก็บงานของคุณ — กด "เลือกโฟลเดอร์…" ถ้าอยากเปลี่ยนที่เก็บ
- รายการโปรเจกต์ที่มีอยู่ พร้อมจุดหกจุดคือด่านตรวจ G1–G6
- ปุ่มเปิดโปรเจกต์ใหม่

---

## 1 · เปิดโปรเจกต์ของตัวเอง

กด **+ เปิดโปรเจกต์ใหม่** แล้วกรอก

| ช่อง | เขียนยังไงให้ถูก |
|---|---|
| ชื่อโครงการ | ชื่อที่คุณจะเรียกมันจริง ๆ |
| รหัสสามตัวอักษร | ใช้แทน `XXX` ในรหัสเอกสารทุกฉบับ เลือกแล้วเปลี่ยนยาก |
| ปัญหาในหนึ่งประโยค | **เขียนเป็นปัญหา ไม่ใช่วิธีแก้** — "นักเรียนลืมว่ามีการบ้านอะไร" ไม่ใช่ "อยากได้แอปจดการบ้าน" |
| ใครเจอปัญหานี้ | เจาะจงกว่าคำว่า "ผู้ใช้" |
| วันนี้เขาทำยังไง | ถ้ายังไม่มีของคุณ เขาแก้ปัญหาด้วยอะไรอยู่ |
| มีเวลากี่วัน | ถ้าไม่รู้ **ปล่อยว่างไว้** |

แล้วเลือก stack กับ git

> **สิ่งที่ต้องสังเกต** — ช่องที่คุณเว้นว่างจะกลายเป็น `TBD — ต้องการ [คน/ข้อมูล]`
> และขึ้นเป็นคำถามค้างใน `project.yml` **ระบบจะไม่เดาแทนคุณ**
> เปิดไฟล์ `project/project.yml` ดูด้วยตาตัวเองว่าเป็นอย่างนั้นจริง

---

## 2 · สั่งงาน แล้วดู

กด **▶ สั่งสายงานทำงาน**

ห้องจะเริ่มขยับ อ่านให้ออกว่าแต่ละอย่างบอกอะไร

| ที่เห็น | มาจากไหน |
|---|---|
| ตัวละครสว่าง | บทบาทนั้นกำลังทำงานอยู่จริง |
| ชื่อไฟล์ในฟีดซ้ายบน | ไฟล์ที่มันเพิ่งเขียน ไม่ใช่ข้อความประดับ · เอาเมาส์ชี้ตัวละครเพื่อดูการ์ดเต็ม |
| `ระยะ 3/7` มุมขวาบน | ระยะที่เท่าไรจากเจ็ด — **ไม่ใช่** เปอร์เซ็นต์ที่เดาว่างานเสร็จไปเท่าไร |
| `tokens · เทิร์น` หัวจอ | ค่าใช้จ่ายจริงของการรันครั้งนี้ |
| แถบล่างซ้าย | ใครกำลังทำงาน นานเท่าไร ลงมือไปกี่ครั้ง |
| ป้ายด่าน G1–G6 ขวาบน | ด่านที่ผ่านแล้ว **ว่างอยู่ = ยังไม่ผ่าน ไม่ใช่ยังไม่ได้ตรวจ** |

ระหว่างรอ **เปิดโฟลเดอร์โปรเจกต์ทิ้งไว้** แล้วดูไฟล์เกิดขึ้นจริง ๆ
สิ่งที่อยู่บนจอกับสิ่งที่อยู่ในดิสก์ต้องตรงกัน ถ้าไม่ตรงคือจอโกหก

---

## 3 · อ่านคำตัดสิน

เมื่อจบ `correcter` จะให้คำตัดสินหนึ่งในสาม

| คำตัดสิน | แปลว่า | คุณต้องทำอะไร |
|---|---|---|
| `PASS` | ตรวจกับของจริงแล้วผ่าน | ไปด่านถัดไป |
| `CORRECTED` | มีข้อบกพร่องที่แก้ได้เอง | แก้แล้วสั่งตรวจใหม่ |
| `BLOCKED` | ต้องใช้คนจริงหรือข้อมูลจากโลกจริง | **หยุด** — หาคนหรือหาข้อมูล |

สั่งตรวจเองได้ทุกเมื่อ

```sh
python3 tools/correcter/verify.py
```

> `BLOCKED` ไม่ใช่ความล้มเหลว และ **ห้ามหาทางอ้อม**
> ห้ามเติมตัวเลขที่ไม่รู้เพื่อให้ผ่าน ห้ามปิดตัวตรวจ ห้ามลดเกณฑ์
> สายงานที่ผ่านได้เสมอ คือสายงานที่ไม่ได้พิสูจน์อะไรเลย

---

## 4 · ห้าอย่างที่ต้องหาให้เจอ แล้วเขียนคำตอบส่ง

ทำระหว่างที่ระบบเดิน ไม่ต้องรอจบ

**4.1** เปิด `project/project.yml` — มี `TBD` กี่จุด และแต่ละจุดต้องใช้ **ใคร** มาตอบ

**4.2** เปิด PRD ที่ได้ — หาข้อกำหนดหนึ่งข้อ แล้วไล่ว่ามันไปโผล่ที่ไหนบ้างใน SRS
ถ้าไล่ไม่เจอ แปลว่าตารางตรวจย้อนกลับขาด นั่นคือข้อบกพร่องที่ต้องรายงาน

**4.3** เปิด `tools/correcter/checks.py` — เลือกตัวตรวจมาหนึ่งตัว
อ่านว่ามัน **วัดอะไรจากไฟล์จริง** แล้วตอบว่า ถ้าตัวตรวจนั้นหาไฟล์ไม่เจอ มันจะตกหรือจะเงียบ

> ข้อนี้สำคัญที่สุดในใบงาน วันที่เราสร้างระบบนี้เจอของจริงว่า ตัวตรวจหลายตัวเคย
> **ฝังรหัสโครงการไว้ในตัวอักษร** พอชี้ไปโปรเจกต์อื่นมันหาเอกสารไม่เจอ คืนรายการว่าง
> แล้วรายงานว่า `VERIFIED` เหมือนเดิม — **ประตูที่เงียบเพราะหาของไม่เจอ อันตรายกว่าประตูที่ตก**
> เพราะประตูที่ตกมีคนไปดู ส่วนประตูที่เงียบไม่มีใครรู้ว่ามันเลิกทำงานไปแล้ว

**4.4** ลองทำให้ด่านตก — แก้เอกสารให้ขัดกันเอง แล้วรัน `verify.py`
ถ้ามันยังผ่าน แปลว่าด่านนั้นวัดไม่จริง เขียนรายงานว่าด่านไหนและเพราะอะไร

**4.5** ดูที่ `open_questions` — เลือกมาหนึ่งข้อ แล้วเขียนว่า
จะได้คำตอบมาจากใคร ด้วยวิธีอะไร ใช้เวลาเท่าไร

---

## 5 · คำสั่งที่ใช้บ่อย

| คำสั่ง | ทำอะไร |
|---|---|
| `/kickoff` | เปิดโปรเจกต์ด้วยการสัมภาษณ์ (ทางเลือกแทนฟอร์ม) |
| `/status` | ตอนนี้โปรเจกต์อยู่ตรงไหน อะไรยัง TBD |
| `/gate` | ตรวจว่าขาดอะไรก่อนไปด่านถัดไป |
| `/doc-new <TYPE>` | สร้างเอกสารพร้อมลงทะเบียนในครั้งเดียว |
| `/cr` | เจอข้อขัดแย้ง ออกคำขอเปลี่ยนแปลง **ไม่ใช่แก้เงียบ ๆ** |
| `/monitor` | เปิดห้องบัญชาการ |

พิมพ์คำสั่งพวกนี้ในช่องแชทมุมขวาล่างของห้องได้เลย บทสนทนาต่อเนื่อง ถามกลับได้

---

## 6 · กติกาที่อยู่เหนือทุกอย่าง

**ไม่รู้ห้ามเดา** — ข้อกำหนดที่แต่งขึ้นแย่กว่าช่องว่าง
ช่องว่างมีคนเห็นแล้วมาเติม แต่ของที่แต่งขึ้นมองไม่เห็น แล้วถูกสร้างต่อไปเรื่อย ๆ

**ด่านที่ผ่านตลอดไม่ใช่ด่าน** — ถ้าหาหลักฐานไม่เจอ แปลว่ายังไม่ผ่าน

**เจอขัดแย้งให้รายงาน อย่าแก้เงียบ** — นักวิเคราะห์เจอข้อกำหนดตีกัน ให้ออก `/cr`
ไม่ใช่เลือกข้างแล้วสร้างต่อ

**คนตรวจไม่แก้ของ** — `code-reviewer` กับ `quality-assurance` ถูกตัดสิทธิ์แก้ไฟล์
ในระดับเครื่องมือ ไม่ใช่แค่ขอความร่วมมือ เปิด `.claude/agents/code-reviewer.md`
ดูบรรทัด `tools:` จะเห็นว่าไม่มี `Write` กับ `Edit` จริง ๆ

---

## ถ้าห้องไม่ขยับ

1. `python3 tools/monitor/emit.py --selftest` — ตัวรับเหตุการณ์ยังดีไหม
2. `cat .monitor/events.jsonl` — hook ส่งถึงหรือเปล่า
3. **Claude Code อ่าน hooks ตอนเปิด session** — ถ้าเปิดค้างไว้ตั้งแต่ก่อนแก้
   `.claude/settings.json` มันจะไม่ส่งอะไรเลย ปิดแล้วเปิดใหม่
